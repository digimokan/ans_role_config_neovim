# ignore case in search patterns
neovim_ignore_case_in_buffer_search: 'true'

# override ignorecase (if true), and search case-sensitive if upper-case input
neovim_override_ignore_buffer_search_case_if_caps: 'true'

